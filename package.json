{
	"name": "delta-fonts",
	"version": "1.0.0",
	"private": true,
	"description": "Public Google Fonts catalog for Delta Font Manager",
	"type": "module",
	"license": "MIT",
	"engines": {
		"node": ">=18"
	},
	"scripts": {
		"sync": "node scripts/sync.mjs",
		"validate": "node scripts/validate.mjs",
		"build:index": "node scripts/build-index.mjs",
		"check": "npm run validate && npm run build:index && git diff --exit-code -- index.json",
		"purge:cdn": "node scripts/purge-jsdelivr.mjs"
	}
}
